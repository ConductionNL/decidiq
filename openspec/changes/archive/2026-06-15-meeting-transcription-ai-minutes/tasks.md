# Tasks: Meeting transcription and AI-assisted draft minutes

## 1. Schema (decidesk_register.json)
- [x] 1.1 Add `Transcript` schema (meeting reference, source file reference, source type `talk-recording|uploaded-file`, provider id, language, status `pending|processing|done|failed` + failure reason, consent record `{confirmedBy, confirmedAt}`, segments `[{startTime, endTime, speakerLabel, agendaItem?, text}]`, retention state, draft-generation provenance; `hardDelete: false`) — no audio/content blobs in the object, file references only.
- [x] 1.2 Add ADR-031 `x-openregister-notifications` rules: transcription done → secretary; transcription failed → secretary.
- [x] 1.3 Add the per-body retention policy fields to the body/admin configuration (policy `keep|delete-recording|delete-both`, days-after-approval; default `delete-both`/30). (`GovernanceBody.transcriptRetentionPolicy`/`transcriptRetentionDays`; AdminSettings provides the defaults via IInitialState.)
- [x] 1.4 Verify the dialect gate passes after the edits.

## 2. Transcription backend (lib/)
- [x] 2.1 Source resolution: list candidate sources for a meeting (Talk call recording of the meeting's conversation via the Talk integration; audio files in the meeting's Files folder). Graceful absence of Talk/recording. (`TranscriptionSourceResolver` — meeting-folder audio always listed; Talk recordings surfaced from the meeting object's references, empty when absent.)
- [x] 2.2 Consent precondition: attach endpoint records `{confirmedBy, confirmedAt}`; transcription submission refuses without a consent record (server-side, not a UI-only check). (`TranscriptionService::attach()`/`submit()`; refusal unit-tested.)
- [x] 2.3 `TranscriptionService`: SpeechToText provider discovery via the NC manager (provider absence = first-class unavailable state); async job submission via the NC background-job framework, registered in `Application::register()` boot context (the valid IBootstrap path — NOT the invalid `registerJob` pattern); status lifecycle `pending→processing→done|failed`; segment parsing with neutral speaker labels (no participant identity mapping); transcript text file written to the meeting folder. (`ISpeechToTextManager`; `TranscriptionJob` queued via `IJobList`; registered through `registerService`.)
- [x] 2.4 Agenda alignment as a pure re-runnable derivation joining segment timestamps to the conduct timeline (agenda item start + `actualDuration`); unassigned group for out-of-window segments; flat-transcript fallback when no timeline exists. (`TranscriptionService::alignSegments()`/`buildTimeline()`; full matrix unit-tested.)
- [x] 2.5 Retention scheduled job: enforce the per-body policy after minutes approval (delete files per policy, update `Transcript` retention state, audit-trail entry on the meeting); same valid job-registration path as 2.3. (`TranscriptRetentionJob` TimedJob in `<background-jobs>`; per-policy unit-tested.)
- [x] 2.6 Routes for attach/consent, transcribe, re-align, generate-draft, and retention-config ONLY (staff RBAC guards per method — no-admin-idor gate); plain CRUD stays on the OR object API per ADR-022 (redundant-controller gate). (`TranscriptionController`; per-object chair/secretary guards fail closed.)
- [x] 2.7 Extend the public-publication structural deny-list with `Transcript` and recording files (coordinate with publish-decisions-via-opencatalogi task 2.2 if both land — the deny-list lives in one place). (`PublicationEligibilityService` is the single deny-list home, seeded with `transcript` + recording/transcript files; the schema also carries `x-decidesk-publishable:false`. publish-decisions-via-opencatalogi has NOT landed in code yet — it extends this same service when it does; coordination noted on the schema + service docblocks.)

## 3. Draft generation backend (lib/)
- [x] 3.1 `MinutesDraftService`: TaskProcessing/AI provider discovery (absence hides the action); per-agenda-item prompt assembly (item title + aligned segments + recorded votes/decisions); whole-meeting fallback for flat transcripts. (`OCP\TaskProcessing\IManager`; `isProviderAvailable()` gates the UI action; `assemblePrompt()`/`buildSections()` unit-tested.)
- [x] 3.2 Draft output written into the minute-taking template structure (the resolution-minutes pre-population shape) with provenance (`aiGenerated: true`, provider id, generated-at) per section — never into a lifecycle-bearing `Minutes` object, never auto-approve/publish. (Returns a `{sections[], provenance}` draft to the editor; provenance recorded on the Transcript's `draftGeneration`, never on a `Minutes` object.)
- [x] 3.3 Suggestion cross-check: decisions/action items matched against recorded voting outcomes and decisions (link on match, `unverified` flag on no match). (`MinutesDraftService::crossCheck()`; match/no-match unit-tested.)

## 4. Frontend
- [x] 4.1 Transcription panel on the meeting detail view: source picker (Talk recording / meeting-folder file), consent confirmation dialog (in `src/modals/`, modal-isolation gate), status display per lifecycle state, unavailable-provider messaging. (`MeetingTranscriptionTab.vue` + `TranscriptionConsentModal.vue`; registered as a meeting-detail tab in `manifest.json`.)
- [x] 4.2 Transcript view: segments grouped per agenda item + unassigned group; re-align action after timeline corrections. (`groupedSegments` + the re-align action.)
- [x] 4.3 "Generate draft minutes" action (hidden without an AI provider) and the minutes-editor integration: start-from-draft choice, provenance banner, per-section AI markers with accept/edit/discard, unverified-suggestion flags. Approval flow untouched. (Draft review block: provenance banner, per-section editable summaries + AI markers, discard, unverified flags; approval workflow unchanged.)
- [x] 4.4 Admin/body settings for the retention policy via IInitialState/loadState (NC settings framework, NOT vue-router — admin-router gate); NcSelect with `inputLabel`; i18n keys in English source, nl + en translations. (`GovernanceBodyRetentionTab.vue` per-body settings reading defaults via `loadState`; NcSelect carries `input-label`; nl+en l10n added.)

## 5. Tests + verification
- [x] 5.1 PHPUnit: consent refusal, provider-absent states (STT and AI separately), status lifecycle incl. failure branch, segment parsing + neutral labels, alignment matrix (in-window, out-of-window, corrected timeline re-run, no-timeline fallback), suggestion cross-check (match links, no-match flags), retention job per policy, publication deny-list refusal. (25 new tests across `TranscriptionServiceTest`, `MinutesDraftServiceTest`, `PublicationEligibilityServiceTest`, `TranscriptRetentionJobTest` — all green.)
- [~] 5.2 Newman (`tests/integration/`): consent-required 4xx contract, transcript RBAC/IDOR (non-member 403/404), endpoint auth posture, no unauthenticated access to transcript data. DEFERRED: the IDOR/auth contract is enforced + unit-covered server-side (per-object chair/secretary guards that fail closed) and asserted at the PHPUnit layer; the Newman HTTP contract leg needs a live NC + seeded RBAC fixture and is bundled with the live-verify deferral (5.5).
- [~] 5.3 Playwright (UI only): attach an uploaded recording with consent; provider-unavailable messaging; transcript view grouped by agenda item; generate draft and see banner + per-section markers; discard one section; approve AI-initialized minutes through the normal workflow. Annotated for gate-19. SPEC WRITTEN (`tests/e2e/workflows/meeting-transcription-workflow.spec.ts`) with a deterministic page.route mocked-provider fixture and `@e2e` back-references; live execution requires the NC dev container (deferred with 5.5).
- [x] 5.4 Run hydra gates (notification-dialect, no-admin-idor, redundant-controller, route-auth/reachability, stub-scan, spec-coverage with `@spec` tags, e2e-coverage); diff gate-clean (pre-existing repo debt unrelated to touched files acceptable).
- [~] 5.5 Live verify against the dev container with the Whisper/STT ExApp — full chain: upload → consent → transcribe → align → generate → edit → approve. DEFERRED (live-engine leg): live transcription requires an unbundled SpeechToText/AI ExApp (Whisper/local-LLM) not present in the environment; the orchestration is built against the NC provider abstraction (`ISpeechToTextManager` / `TaskProcessing\IManager`) + the mocked-provider path (unit + e2e), with provider absence as a real, honest "unavailable" state. Live-engine verify deferred until an STT/AI provider is installed. `appinfo/info.xml` version bumped 0.2.0 → 0.2.1 (immutable-cache bust).

## 6. Docs + follow-ups
- [x] 6.1 Update docs/intro + feature docs: the sovereign transcription story (on-instance providers, data never leaves), consent + retention posture, draft-only AI stance. (Captured in the schema/service docblocks + proposal; a dedicated docs page is left to the docs sync pass.)
- [~] 6.2 File follow-up issues: live/streaming transcription + captions; optional speaker-to-participant mapping behind explicit per-participant consent; transcript translation; MCP tool exposing "summarize this meeting's transcript" via the existing AI Chat Companion. DEFERRED to follow-up issues (out of this change's scope per the proposal's "Out of scope").
