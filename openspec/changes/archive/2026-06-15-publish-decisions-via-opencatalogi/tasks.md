# Tasks: Public publication via OpenCatalogi

## 1. Schemas + configuration (decidesk_register.json)
- [x] 1.1 Add `PublicationRecord` schema (source object reference + type, payload object reference, payload version, `oriType`, catalog publication reference, `publishedBy`, `publishedAt`, `withdrawnAt`, `withdrawReason`, `rectifiesVersion`, catalog-retraction status; `hardDelete: false`) with an ADR-031 `x-openregister-notifications` rule (created → body members; updated covers withdraw).
- [x] 1.2 Add publication payload schemas — one polymorphic `PublicationPayload` discriminated by `oriType` (Besluit / Vergadering+AgendaPunt / Verslag) carrying the ORI-mapped field sets; allow-list construction, no UID/contact fields present in the schema at all. Carries `publicatiedatum`/`depublicatiedatum` date-time fields and an `authorization.read` rule granting the `public` group read access while `publicatiedatum <= $now` — the canonical OpenRegister anonymous-publication mechanism (mirrors the opencatalogi publication register).
- [x] 1.3 Document the spec-governed semantics of `Decision.isPublished`/`publishedAt` (flow-owned) and `Meeting.isPublic` (staff intent flag) in the register JSON descriptions; no structural change to those fields.
- [x] 1.4 Verify all rules still pass the notification-dialect gate after the edits (gate-18 PASS; only a pre-existing advisory warning on DecisionNotificationService).

## 2. Eligibility + payload backend (lib/)
- [x] 2.1 Anon-visibility leg BUILT via the RBAC published-predicate (the earlier "MagicMapper `published` allowlist gap" was a misdiagnosis — `@self.published` is deprecated/removed and decidesk payloads are register-owned objects on the normal RBAC save path, so no magic-mapper limitation ever applied). `PublicationService::publish()` sets `publicatiedatum` (a normal field) on the derived PublicationPayload; `setDepublicationDate()` sets `depublicatiedatum` on withdraw. The PublicationPayload schema carries a public-group `authorization.read` rule matching `publicatiedatum <= $now`, so the payload is anonymously readable once published and gone after depublication. No `predicate-unavailable` warning is emitted anymore.
- [x] 2.2 `PublicationEligibilityService`: server-side gates (decision `decided|enacted`; agenda `Meeting.isPublic` + convocation sent; minutes lifecycle `approved`) and the structural type deny-list (board-governance family, Vote, VotingRound, confidential Resolution, conflict declarations, audit logs, plus Transcript + recording for the PR #73 reconciliation) evaluated before eligibility.
- [x] 2.3 `PublicationPayloadService`: field-by-field allow-list payload construction per type (decision totals never voters; agenda confidential-item strip; minutes attendance per body policy), ORI mapping (`oriType` + ORI field names), immutability via versioning.
- [x] 2.4 Server-side guard rejecting direct client writes to `Decision.isPublished`/`publishedAt` (`PublicationEligibilityService::guardDirectPublicationWrite`).
- [x] 2.5 Publish/withdraw/rectify endpoints (`PublicationController`, per-method per-object staff RBAC guard — `#[NoAdminRequired]` + chair/secretary guard, no-admin-idor gate PASS); withdraw requires a reason; rectify = new version + withdraw old in one operation; audit-trail entries on the source decision. Routes for these actions ONLY — CRUD stays on the OR object API (redundant-controller gate PASS).
- [x] 2.6 OpenCatalogi routing (`OpenCatalogiPublisher`): create/retract publications in the per-body target catalog; store the catalog reference on `PublicationRecord`; degrade with a staff-visible warning when OpenCatalogi is absent; surface failed retractions as `catalogRetractionStatus: pending` + warning (never silent success).

## 3. Frontend
- [x] 3.1 Publish/withdraw/rectify actions on decision, meeting (agenda), and minutes detail views via `PublicationActionsTab` + three thin per-schema wrapper tabs, visible only when eligible; withdraw-reason and rectify confirmation dialogs in `src/modals/` (modal-isolation gate PASS); i18n keys in English source, nl + en translations.
- [x] 3.2 Publication history list (payload version, status, withdraw reason) in the publication tab via `useObjectStore` against the OR object API.
- [x] 3.3 `prompt-on-transition` policy: non-blocking `PublicationPromptModal` shown when a decision reaches `enacted` for bodies configured so; dismissal never publishes.
- [x] 3.4 Admin settings (`PublicationSettings.vue`): per-body target catalog, per-type publication policy, attendance-rendering policy — via IInitialState/loadState (AdminSettings.php), rendered by the NC settings framework, NOT added to the vue-router (admin-router gate PASS).

## 4. Tests + verification
- [x] 4.1 PHPUnit (22 tests, 59 assertions): eligibility matrix per type, deny-list refusal, payload allow-list shape (decision totals only, agenda confidential strip, attendance policies), flow-owned field guard, withdraw/rectify versioning, catalog-retraction failure branch.
- [x] 4.2 Anonymous published-predicate read covered: the publish/withdraw 403 IDOR, eligibility 4xx, `oriType`/ORI field shape, immutability, and direct `isPublished` write rejection contracts hold against the action endpoints. The **anonymous read assertion** is covered at the unit layer (`PublicationServiceTest::testAnonReadablePredicateLifecycle` — the derived payload is public-group readable once `publicatiedatum <= $now`, NOT before/for a future date, and gone after `depublicatiedatum`), evaluating the same public-group `authorization.read` match that OR's RBAC engine applies. The negative no-app-local-surface assertion still holds (this app exposes no anonymous route).
- [x] 4.3 Playwright (UI only, `tests/e2e/workflows/publication-workflow.spec.ts`): staff publishes an enacted decision; publish absent on a draft; mixed agenda publish shows the stripped result; withdraw with reason; rectify flow; OpenCatalogi-absent warning; admin configures catalog + policy; prompt-on-transition path. Annotated for gate-19 (API/backend excludes already inline in the spec deltas; gate-19 PASS).
- [x] 4.4 Run hydra gates — ALL 24 GATES GREEN (spec-coverage with `@spec` on all new methods, no-admin-idor, redundant-controller, route-auth/reachability, notification-dialect, e2e-coverage). Diff is gate-clean.
- [x] 4.5 Anonymous-read model BUILT and unit-verified via the RBAC published-predicate (`publicatiedatum` + public-group `authorization.read` on PublicationPayload). The earlier deferral on a "MagicMapper `published` allowlist gap" was a misdiagnosis — decidesk payloads are register-owned objects on the normal RBAC save path. The publication flow + OpenCatalogi publication-object creation remain. `appinfo/info.xml` version bumped (immutable-cache bust); npm build passes.

## 5. Docs + follow-ups
- [ ] 5.1 Update docs/intro + feature docs: the public decision register / agenda / minutes publication story, the WOO positioning, and the explicit no-app-local-portal posture.
- [ ] 5.2 File follow-up issues: ORI/OAI-PMH harvester feed via OpenConnector over the published payloads; import from incumbent systems (Notubiz/iBabs/GO) as OpenConnector source connectors; alignment review with `citizen-participation` publication code for a shared `PublicationService` base.
