# configuration-surface tasks

## 1. Give the configurable types a home

- [x] 1.1 Index and detail pages for `meeting-type`, `agenda-item-type`, `position-type` and `governance-body-composition`.
- [x] 1.2 Show what uses a type on its detail page.
- [x] 1.3 Place all four in the settings foldout, leaving the primary menu at six.

## 2. Clear the rules that point at nothing

- [x] 2.1 Remove the seven relocations naming menu entries that no longer exist.
- [x] 2.2 Add a test asserting every layout rule still names a real entry.
- [x] 2.3 Prove that test fails on a planted stale rule.

## 3. Prove it

- [ ] 3.1 E2E: each configuration surface is reachable and lists its rows.
