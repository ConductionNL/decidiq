<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="tests/bootstrap-unit.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         executionOrder="depends,defects"
         requireCoverageMetadata="false"
         beStrictAboutCoverageMetadata="true"
         beStrictAboutOutputDuringTests="true"
         failOnRisky="true"
         failOnWarning="false"
         failOnDeprecation="false">

    <testsuites>
        <testsuite name="Unit Tests">
            <directory>tests/Unit</directory>
            <directory>tests/unit</directory>
        </testsuite>
        <testsuite name="Integration Tests">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory suffix=".php">lib/</directory>
        </include>
        <exclude>
            <directory>lib/Migration/</directory>
            <file>lib/AppInfo/Application.php</file>
            <!--
                Same category as lib/Migration/ above: a repair step whose body is
                ALTER TABLE / UPDATE against shard tables resolved at runtime. It
                cannot be unit-tested — mocking IDBConnection needs doctrine/dbal,
                which this app does not install, and OCP's IQueryBuilder references
                Doctrine\DBAL\ParameterType, so createMock() throws before any
                assertion runs. run()/shardTables()/columnsOf()/exec() therefore
                have no reachable unit path.

                Excluded from MEASUREMENT only. The decision logic IS tested:
                tests/Unit/Repair/RenameDutchVocabularyColumnsTest.php pins that
                another register's shard is never matched — which is the whole
                point of scoping this step by register, since `onderwerp` also
                exists in procest — plus the toelichting/omschrijving split and the
                map invariants. Those tests still run, and go red when the guard is
                removed.
            -->
            <file>lib/Repair/RenameDutchVocabularyColumns.php</file>
        </exclude>
    </source>

    <coverage>
        <report>
            <clover outputFile="coverage/clover.xml"/>
            <html outputDirectory="coverage/html"/>
            <text outputFile="php://stdout" showUncoveredFiles="false"/>
        </report>
    </coverage>

    <php>
        <env name="PHPUNIT_RUN" value="1"/>
    </php>
</phpunit>
